# popshop_patternlibrary

To run locally, 
1. Install simple-server
npm install simple-server

2. Run simple-server
simple-server

3. Put the local server address in your browser to view
ex. http://localhost:3000
